spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pr_management  # Replace with your database URL
    username: root                             # Replace with your database username
    password: 80213280@Study                            # Replace with your database password
    driver-class-name: com.mysql.cj.jdbc.Driver           # MySQL JDBC Driver
  jpa:
    hibernate:
      ddl-auto: update                                     # Use 'create', 'create-drop', 'update', or 'none'
    show-sql: true                                        # Show SQL queries in the console
    properties:
      hibernate:
        format_sql: true                                   # Format SQL queries
  logging:
    level:
      org.hibernate.SQL: DEBUG                            # Log SQL queries
      org.hibernate.type.descriptor.sql.BasicBinder: TRACE # Log SQL bind parameters
